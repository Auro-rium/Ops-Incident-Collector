services:
  opsincident-collector:
    build: ..
    command: ["inspect", "--path", "/data"]
    environment:
      INCIDENTOPS_API_URL: "http://host.docker.internal:8001"
      INCIDENTOPS_PROJECT_ID: "proj_123"
    volumes:
      - ./:/data:ro
